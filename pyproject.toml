[project]
name = "captive-portal"
version = "0.2.0"
description = "Minimal captive portal with iptables/ipset + FastAPI"
requires-python = ">=3.11"

dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "jinja2>=3.1",
]
