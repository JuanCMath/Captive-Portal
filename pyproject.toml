[project]
name = "captive-portal"
version = "0.3.0"
description = "Portal cautivo con iptables/ipset, backend HTTP propio (solo librería estándar)"
requires-python = ">=3.11"

# El backend (app/) no usa FastAPI/uvicorn/jinja2: implementa su propio
# servidor HTTP sobre sockets con la librería estándar. Sin dependencias
# de terceros en tiempo de ejecución.
dependencies = []
